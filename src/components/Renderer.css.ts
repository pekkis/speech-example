import { style } from "@vanilla-extract/css";

export const isUtteringClass = style({
  backgroundColor: "rgb(255, 100, 100, 80%)"
});

export const imgClass = style({
  maxWidth: "100%"
});

export const articleClass = style({
  backgroundColor: "rgb(255, 255, 255, 75%)",
  borderRadius: "10px",
  margin: "1em 0 2em 0",
  padding: "2em"
});

export const selectClass = style({
  maxWidth: "10em"
});
